# SQLite mặc định — file auction.db sẽ được tạo ở thư mục chạy server.
# Không cần password, không cần daemon.

db.url=jdbc:sqlite:auction.db
db.username=
db.password=
db.pool.maxSize=4

server.port=1234
server.maxClients=100
